{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "scripts": {
    "build": "react-scripts build"
  },
  "routes": [
    {
      "src": "^/(?!static/|api/|favicon\\.ico|robots\\.txt|manifest\\.json).*$",
      "dest": "/index.html"
    }
  ]
}
